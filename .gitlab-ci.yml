image: gitlab/gitlab-ce:latest 

before_script:
    - apt-get update && apt-get upgrade -y
    - apt-get install python2.7 python-pip netcat -y 
    - bash tests/configure_gitlab.sh
    - pip install -r requirements.txt

all_tests:
    script:
    - bash tests/functional_tests.sh
